# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=gobject-introspection
pkgver=1.54.1
pkgrel=4
pkgdesc="The GObject Introspection is used to describe the program APIs and collect them in a uniform, machine readable format."
arch=('x86_64')
url="https://ftp.gnome.org/pub/gnome/sources/gobject-introspection/1.54"
license=('GPL')
groups=('desktop')
source=("$url/$pkgname-$pkgver.tar.xz")
sha256sums=('b88ded5e5f064ab58a93aadecd6d58db2ec9d970648534c63807d4f9a7bb877e')
depends=('rootfs')

build() {
   cd "$srcdir/$pkgname-$pkgver"

   ./configure --prefix=/usr --disable-static

   make
}

package() {
   cd "$srcdir/$pkgname-$pkgver"

   make DESTDIR="$pkgdir" install
}

