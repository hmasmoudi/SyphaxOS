# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=yelp-xsl
pkgver=3.20.1
pkgrel=4
pkgdesc="The Yelp XSL package contains XSL stylesheets that are used by the Yelp help browser to format Docbook and Mallard documents."
arch=('x86_64')
url="http://ftp.gnome.org/pub/gnome/sources/yelp-xsl/3.20"
license=('GPL')
groups=('desktop')
source=("$url/${pkgname}-${pkgver}.tar.xz")
sha256sums=('dc61849e5dca473573d32e28c6c4e3cf9c1b6afe241f8c26e29539c415f97ba0')
depends=('rootfs')

build() {
   cd "$srcdir/$pkgname-$pkgver"

   ./configure --prefix=/usr
   
   make
}

package() {
   cd "$srcdir/$pkgname-$pkgver"

   make DESTDIR="$pkgdir" install
}

