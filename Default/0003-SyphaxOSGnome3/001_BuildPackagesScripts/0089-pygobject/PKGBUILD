# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=pygobject
pkgver=3.26.0
pkgrel=4
pkgdesc="PyGObject 2 and 3 provides Python bindings to the GObject class from GLib"
arch=('x86_64')
url="http://ftp.gnome.org/pub/gnome/sources/pygobject/3.26"
license=('GPL')
groups=('desktop')
source=("$url/${pkgname}-${pkgver}.tar.xz")
sha256sums=('7411acd600c8cb6f00d2125afa23303f2104e59b83e0a4963288dbecc3b029fa')
depends=('rootfs')

prepare() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   mkdir python2
   mkdir python3
}

build() {
   cd "$srcdir/${pkgname}-${pkgver}"

   pushd python2
   ../configure --prefix=/usr --with-python=/usr/bin/python
   make
   popd
   
   pushd python3
   ../configure --prefix=/usr --with-python=/usr/bin/python3
   make
   popd
}

package() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   make -C python2 DESTDIR="$pkgdir" install
   make -C python3 DESTDIR="$pkgdir" install
}

