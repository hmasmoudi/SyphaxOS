# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=pygobject
pkgver=3.24.1
pkgrel=1
pkgdesc="PyGObject 2 and 3 provides Python bindings to the GObject class from GLib"
arch=('x86_64')
url="http://ftp.gnome.org/pub/gnome/sources/pygobject/3.24"
license=('GPL')
groups=('desktop')
source=("$url/${pkgname}-${pkgver}.tar.xz")
sha256sums=('a628a95aa0909e13fb08230b1b98fc48adef10b220932f76d62f6821b3fdbffd')
depends=('rootfs>=2.0.0')

prepare() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   mkdir python2
   mkdir python3
}

build() {
   cd "$srcdir/${pkgname}-${pkgver}"

   pushd python2
   ../configure --prefix=/usr --with-python=/usr/bin/python
   make
   popd
   
   pushd python3
   ../configure --prefix=/usr --with-python=/usr/bin/python3
   make
   popd
}

package() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   make -C python2 DESTDIR="$pkgdir" install
   make -C python3 DESTDIR="$pkgdir" install
}

