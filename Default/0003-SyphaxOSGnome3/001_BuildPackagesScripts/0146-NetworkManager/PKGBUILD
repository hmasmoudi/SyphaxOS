# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=NetworkManager
pkgver=1.6.2
pkgrel=1
pkgdesc="NetworkManager is a set of co-operative tools that make networking simple and straightforward."
arch=('x86_64')
url="http://ftp.gnome.org/pub/gnome/sources/NetworkManager/1.6"
install=${pkgname}.install
license=('GPL')
groups=('desktop')
source=("$url/${pkgname}-${pkgver}.tar.xz"
        "${pkgname}.install")
sha256sums=('b22b6f55cde37bec4982f9be4b1808a21101d807a05049c670116d95059a26f9'
            '2de1347fa92bab517619f7a9c82de8258075eb2229fa150cce3fd9dda8215af4')
depends=('rootfs>=2.0.0')

prepare() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   sed -i "/seems to be moved/s/^/#/" build-aux/ltmain.sh
}

build() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   CXXFLAGS="-O2 -fPIC"                                        \
   ./configure --prefix=/usr                                   \
               --sysconfdir=/etc                               \
               --localstatedir=/var                            \
               --with-nmtui                                    \
               --disable-ppp                                   \
               --disable-json-validation                       \
               --with-session-tracking=systemd                 \
               --with-systemdsystemunitdir=/lib/systemd/system \
               --docdir=/usr/share/doc/network-manager-1.6.2
   
   make
}

package() {
   cd "$srcdir/${pkgname}-${pkgver}"
   
   mkdir -pv "${pkgdir}"/{var,run}
   ln -snfv ../run "${pkgdir}"/var/run
   
   make DESTDIR="${pkgdir}" install
   
   rm -rf "${pkgdir}"/var/run
}

