# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=glib-networking
pkgver=2.54.0
pkgrel=4
pkgdesc="The GLib Networking package contains Network related gio modules for GLib."
arch=('x86_64')
url="http://ftp.gnome.org/pub/gnome/sources/glib-networking/2.54"
license=('GPL')
groups=('desktop')
source=("$url/${pkgname}-${pkgver}.tar.xz")
sha256sums=('5961b3779080b72314b373ff5d4790eb7e41b75ca91816ad7a81ef32922f7096')
depends=('rootfs')

build() {
   cd "$srcdir/$pkgname-$pkgver"

   ./configure --prefix=/usr                                 \
               --with-ca-certificates=/etc/ssl/ca-bundle.crt \
               --disable-static

   make
}

package() {
   cd "$srcdir/$pkgname-$pkgver"

   make DESTDIR="$pkgdir" install
}

