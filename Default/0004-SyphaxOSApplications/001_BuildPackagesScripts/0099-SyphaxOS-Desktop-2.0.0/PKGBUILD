# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=SyphaxOS-Desktop
pkgver=2.2.0
pkgrel=4
pkgdesc="SyphaxOS 2.2.0 Default Desktop Package based on Gnome Desktop."
arch=('x86_64')
install=${pkgname}.install
license=('GPL')
groups=('desktop')
depends=(
         'gnomedesktop>=2.2.0'
         'pacman'
         'gnome-calculator'
         'gedit'
         'eog'
         'gnome-screenshot'
         'file-roller'
         'p7zip'
         'totem'
         'cheese'
         'transmission'
         'evince'
         'skypeforlinux'
         'rhythmbox'
         'ttf-ms-fonts'
         'wps-office'
         'budgie-desktop'
         'google-chrome'
         'flatpak'
         )

prepare() {
   mkdir -pv "$srcdir/$pkgname-$pkgver"
}

package() {
   cd "$srcdir/$pkgname-$pkgver"
   
   echo "Packaging SyphaxOS Desktop"
}

