# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=thunderbird
pkgver=45.6.0
pkgrel=2
pkgdesc="Thunderbird is a free email application that’s easy to set up and customize - and it’s loaded with great features!"
arch=('x86_64')
url="https://ftp.mozilla.org/pub/thunderbird/releases/45.6.0/linux-x86_64/fr"
license=('GPL')
groups=('applications')
source=("$url/${pkgname}-${pkgver}.tar.bz2"
        "thunderbird.desktop")
md5sums=('e1ef36f510f3a6320face02db3944864'
         '634d886c8dcb194702858e6103f6f176')
depends=('rootfs>=2.0.0')

prepare() {
   cd "$srcdir/${pkgname}"
}

build() {
   cd "$srcdir/${pkgname}"
}

package() {
   cd "$srcdir/${pkgname}"
    
   mkdir -pv "${pkgdir}"/opt/
   mkdir -pv "${pkgdir}"/usr/share/applications/
   
   cp -rf "$srcdir/${pkgname}" "${pkgdir}"/opt/
   cp -rf ../../thunderbird.desktop "${pkgdir}"/usr/share/applications/thunderbird.desktop
}

