# Maintainer: Hatem Masmoudi <hatem.masmoudi@gmail.com>

pkgname=evolution-ews
pkgver=3.24.2
pkgrel=1
pkgdesc="MS Exchange integration through Exchange Web Services"
url="http://ftp.gnome.org/pub/gnome/sources/evolution-ews/3.24"
arch=(i686 x86_64)
license=(LGPL2.1)
depends=(libmspack evolution)
options=(!emptydirs)
source=("$url/${pkgname}-${pkgver}.tar.xz")
sha256sums=('31eb84a204a50b31932696c03394a7d2563ec2c3eefa52a802e63d0346a28851')

build() {
  cd $pkgname-$pkgver
  cmake -DCMAKE_INSTALL_PREFIX=/usr -DLIBEXEC_INSTALL_DIR=/usr/lib -DSYSCONF_INSTALL_DIR=/etc -DENABLE_STATIC=OFF .
  
  make
}

package() {
  cd $pkgname-$pkgver
  make DESTDIR="$pkgdir" install
}
